<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <% include ./partials/head.ejs %>
</head>

<body>
  <% include ./partials/header.ejs %>
  <div class="row">
    <div class="col s4">
      <%if(user){%>
      <%if(user.username===competition.arbiter_name){%>
      You are currently the arbiter of this competition. You may select a new arbiter below:
      <br>
      <form action='/competitions/<%=competition.id%>/arbiter' method='post'>
        <input type="text" name="arbiter" list="usernames">
        <datalist id="usernames">
          <%for(let i = 0; i < users.length; i++){%>
          <option value='<%=users[i].username%>'>
            <%}%>
        </datalist>
        <button>Submit</button>
    </div>
    <div class="col s6">
      </form>
      <a href='/competitions/<%=competition.id%>/complete'>Mark Complete</a>
      <%}%>
      <%}%>
      <%=competition.title%> <br>
      Started:
      <%=competition.created_at%><br>
      Description:
      <%=competition.description%><br>
      Minimum:
      <%=competition.bet_min%> <br>
      Pool:
      <%=competition.pool%><br>
      Arbiter:
      <%=competition.arbiter_name%><br>
      Participants:
      <%for(let i = 0; i < participants.length; i++){%>
      <%=participants[i].username%>
      <%}%>
      <br>
    </div>
  </div>
  <b>Comments:</b>
  <%for(let i = 0; i < comments.length; i++){%>
  <br>
  <%=comments[i].username%>:
  <%=comments[i].content%>
  <%}%> <br>

  <%if(user){%>
  <a href='/competitions/<%=competition.id%>/join'>Join Competition</a>
  </div>
  <form action='/competitions/<%=competition.id%>/comment' method='POST'>
    <div class='input-field'>
      <textarea class='materialize-textarea' name='content'></textarea>
      <button type='submit'>Submit Comment</button>
    </div>
  </form>
  <%}%>

</body>

</html>